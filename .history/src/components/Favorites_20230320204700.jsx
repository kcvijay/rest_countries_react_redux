import React from "react";
import { useSelector, useDispatch } from "react-redux";

const Favorites = () => {
  return (
    <div>
      <h2>favorites</h2>
    </div>
  );
};

export default Favorites;
